# See https://wiki.hypr.land/0.54.0/Configuring/Window-Rules/

windowrule {
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = on
}

windowrule {
    name = move-hyprland-run
    match:class = hyprland-run

    move = 20 monitor_h-120
    float = on
}

# TUI popup (impala, bluetui, etc.)
windowrule {
    name = tui-popup
    match:class = tui-popup

    float = on
    size = 800 600
    center = on
    pin = on
}
